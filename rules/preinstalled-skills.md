# Preinstalled Skills Rule

The source of truth is:

- source list: `skills_list.md`
- generated manifest: `manifests/preinstalled-skills.json`

When Jinyiwei is installed into OpenClaw, the skills in the manifest must be installed into the target OpenClaw workspace.

